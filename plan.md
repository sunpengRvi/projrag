阶段一：数据检索层 —— 企业级向量微服务 (进程 A, 绑定 8002 端口)
这是整个 RAG 系统的“记忆中枢”，负责离线数据处理和在线高速检索，并剥离了所有与外部直接交互的逻辑。

数据接入与切分： 批量读取 GitHub 上克隆的 Markdown 源文件，使用按标题层级切分（Header-based Chunking）策略，确保代码块和技术上下文的完整性。

向量化与企业级存储： 使用 BGE-m3 模型将文本向量化，并抛弃轻量级玩具库，直接接入支持百亿级扩展的企业级向量数据库 Milvus（可使用其单机兼容的 Milvus Lite 模式）。

检索策略： 实现混合检索（向量相似度 + 关键词 BM25），确保 asyncio 等专有名词被精准召回。

内部通信 (核心亮点)： 不提供常规的 HTTP 接口，而是定义 .proto 文件，通过 gRPC 协议向外提供 SearchDocuments 检索服务，实现极低延迟的二进制数据传输。

阶段二：核心算力层 —— 高吞吐 vLLM 推理引擎 (进程 B, 绑定 8001 端口)
这是系统的“大脑”，作为纯粹的计算黑盒运行，只负责将算力压榨到极致。

模型部署： 使用 vllm-metal 框架，部署参数量小但指令遵循极强的 Qwen2.5-3B-Instruct 模型，充分利用 Apple Silicon 的统一内存架构。

企业级特性： 开启 vLLM 的连续批处理 (Continuous Batching) 功能，以应对潜在的高并发请求。

可观测性准备： 启动时暴露 Prometheus 标准的 /metrics 监控接口。

阶段三：业务中枢层 —— 异步 API 总网关 (进程 C, 绑定 8000 端口)
这是系统唯一对外的“门面”，负责流量接收、逻辑编排和协议转换。

接口开发： 使用 FastAPI 提供标准的 RESTful API 接口，供外部用户或前端调用。

异步编排 (Orchestration)： 充分利用 Python 的异步特性。接收用户问题后，网关首先作为 gRPC 客户端，极速调用 8002 端口获取检索文档；组装 Prompt 后，再通过 HTTP 异步调用 8001 端口的 vLLM 推理接口。

流式输出： 将大模型生成的回答通过 Server-Sent Events (SSE) 协议，以打字机流式效果实时推回给外部用户。

阶段四：系统评估与全链路可观测性 (后台守护进程)
这是保障系统持续优化的“仪表盘”，也是面试中最能体现 Senior 视角的加分项。

动态监控探针： 编写一个 Python 守护进程，定时轮询 8001 端口的 /metrics 指标。利用 Matplotlib 实时绘制系统并发量、大模型首字延迟 (TTFT) 以及 KV Cache 占用率的动态折线图。

RAG 效果评估： 准备 20 个硬核的后端开发与算法原理问题（如“深拷贝与浅拷贝的内存机制差异”），测试并量化系统缓解大模型“幻觉”的实际效果。